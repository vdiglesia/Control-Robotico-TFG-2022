using UnityEngine;

namespace RobotConnector.Scripts
{
    public class TesterAdapter : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
